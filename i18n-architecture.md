# Internationalization architecture

PT-BR is the only published language in this cycle. Static route structure allows a future reviewed English version under `/en/`. Do not publish machine-only translations. When `/en/` is introduced, add reciprocal `hreflang="pt-BR"` and `hreflang="en"` links and language-specific canonical metadata.
